# Vendored Dependencies

This directory contains upstream source clones used for optional ImPlot-enabled WASM builds.

- `imgui`: `https://github.com/ocornut/imgui.git`
- `implot`: `https://github.com/epezent/implot.git`

Use with:

```bash
NBIMPLOT_WITH_IMPLOT=ON scripts/build_wasm.sh
```
